[phases.setup]
nixPkgs = ["python311", "pkg-config", "mariadb"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "python manage.py migrate && daphne -b 0.0.0.0 -p $PORT nexus_project.asgi:application"
